# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0

# Data / ORM
SQLAlchemy==2.0.30
pydantic==2.7.4
pydantic-settings==2.3.4
# PostgreSQL driver — REQUIRED in production. SQLite corrupts under the
# automation's concurrent load on Azure ("database disk image is malformed") and
# is ephemeral there; set DATABASE_URL=postgresql://... to use Postgres.
psycopg2-binary==2.9.9

# File parsing / imports
python-multipart==0.0.9
# Principal document ingestion (resume / case studies / bios)
pypdf==4.2.0
python-docx==1.1.2

# HTTP client for integrations (Apollo, Twilio, etc.)
httpx==0.27.0

# AI insight + personalization engine
anthropic==0.39.0
