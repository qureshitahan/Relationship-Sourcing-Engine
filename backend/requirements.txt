# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0

# Data / ORM
SQLAlchemy==2.0.30
pydantic==2.7.4
pydantic-settings==2.3.4
# Postgres driver (Azure Database for PostgreSQL). psycopg2-binary has no
# prebuilt wheel for newer Python yet and needs a C compiler to build from
# source; psycopg (v3) ships a working wheel and is the actively maintained
# driver, so use that instead. DATABASE_URL must use the postgresql+psycopg://
# scheme (not bare postgresql://) so SQLAlchemy picks this driver.
psycopg[binary]==3.3.4

# File parsing / imports
python-multipart==0.0.9
# Principal document ingestion (resume / case studies / bios)
pypdf==4.2.0
python-docx==1.1.2

# HTTP client for integrations (Apollo, Twilio, etc.)
httpx==0.27.0

# AI insight + personalization engine
anthropic==0.39.0

# IANA timezone database. Campaign send timing resolves each recipient's zone
# via zoneinfo, which reads the OS tz files — present on Linux, absent on
# Windows. Without this a dev machine raises inside the send stage and takes the
# whole autopilot run down with it.
tzdata==2024.1
